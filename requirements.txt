fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
httpx==0.27.2
python-dotenv==1.0.1
sqlalchemy==2.0.35

# Optional (improves sentence splitting if installed with a model, e.g.
#   pip install spacy && python -m spacy download en_core_web_sm
# The app works perfectly well without either of these).
# spacy==3.7.5
